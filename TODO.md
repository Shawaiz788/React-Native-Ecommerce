- [x] Inspect Android Gradle files and locate where Java/JDK is configured.
- [ ] Fix Gradle/JDK mismatch by forcing Gradle to use the JDK 17 installation path (update android/gradle.properties).
- [ ] Re-run `npx expo run:android` and confirm build passes.

